/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Add this line
  // images: { unoptimized: true } // Usually required for static exports
};

module.exports = nextConfig;